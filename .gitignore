# Secrets
.env
.env.*
!.env.example

# Dependencies / build
node_modules
.svelte-kit
build
dist
*.tsbuildinfo

# Private dataset — NOT part of the public hackathon repo
inputs

# The mock "gym" (mock NetSuite ERP + eval scenarios + fixtures) IS public so the
# eval is reproducible. Only its build-time reference — distilled from the private
# inputs/ — stays out of the public tree.
gym/REFERENCE.md

# Eval run artifacts (transcripts, screenshots, judge reports)
runs
.runs
.design-tmp
.playwright-mcp

# OS / tooling
.DS_Store
.venv
__pycache__
*.pyc

# Frontend-design exploration mockups (the chosen design is implemented in ui/)
mockups
